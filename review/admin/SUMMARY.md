# SUMMARY — Review Bao Mat + On Dinh App Flutter Admin (AMZ Xanh)

> **Code duoc review:** release commit `3e6d332` ("fix: add device info privacy manifest"), nhanh `main`, ngay 07-08/06/2026.
> **Pham vi:** 112 file `.dart` trong `amz_xanh_admin_app/lib` + config `android/` & `ios/`.
> **Ket luan tong:** Ban code SAU outsource fix da xu ly DUT DIEM phan lon loi CRITICAL cu (mat khau plaintext, log credential, cleartext, anh dummy). Hien KHONG con CRITICAL. Con lai vai diem MEDIUM/LOW.

---

## 1. Tong quan

Day la app completeness-critic / recon. LUU Y QUAN TRONG: cac file module review (M1-M6) va deep-dive (D1-D5) ma de bai gia dinh da ton tai KHONG co thuc trong `docs/review/admin/` (thu muc rong truoc khi chay). Vi vay agent nay da tu thuc hien review goc tren toan bo 112 file thay vi chi doi soat.

Kien truc bao mat loi (post-fix) rat tot:
- Token: luu trong `flutter_secure_storage` (Keychain/Keystore), KHONG con o SharedPreferences. `init()` chu dong xoa gia tri plaintext legacy.
- Remember Me: chi luu email; `getRememberMePassword()` luon tra `null`; password bi xoa khoi prefs. -> Loi "luu mat khau plaintext" cu DA HET.
- Logout / het phien: `StorageService.logout()` xoa ca secure storage lan prefs + reset cache RAM. `AdminApiResponse` tu dong dieu huong ve login khi gap 401/403 hoac code token-expired (guard chong goi trung).
- Transport: Android `usesCleartextTraffic="false"`, iOS `NSAllowsArbitraryLoads=false`. Certificate pinning (SHA256) ap cho host portal qua `SecureHttpClient`; fingerprint nap tu env `AMZ_CERT_SHA256` (khong hardcode), thieu config thi NEM exception (fail-closed).
- Config: URL/secret nap qua `String.fromEnvironment`, default HTTPS. KHONG co secret hardcode.
- Logging: `logger` global tat hoan toan o release (`Level.off`). Cac `debugPrint` deu boc trong `kDebugMode`. Khong thay log token/password/body.
- Login: device ID tu `device_info_plus`, fallback `Random.secure`; co khoa dang nhap sau 5 lan sai (30s); `mounted` guard day du sau moi `await`; `requestId` dung `Random.secure`.

Toan bo HTTP nghiep vu di qua `SecureHttpClient` -> pinning ap nhat quan. Khong co loi goi `http.get/post` thuat song. Khong co WebView, deeplink, dummy image data.

## 2. Do phu (coverage)

- 112/112 file `.dart` nam trong pham vi review. orphanFiles = rong.
- D1 secrets: PASS. D2 token/authz: PASS (xem MEDIUM authz client). D3 logging-leak: PASS. D4 transport: PASS. D5 crash: PASS phan lon.
- Android & iOS config DA soi (`AndroidManifest.xml`, `Info.plist`). Chua co `network_security_config.xml` rieng (khong bat buoc vi cleartext off + pinning o tang code).

## 3. CRITICAL + HIGH con lai

- CRITICAL: 0
- HIGH: 0

### MEDIUM
1. [auth-authz] Khong co guard phan quyen phia client — `main_layout.dart:23-25` hien toan bo nav cho moi user, khong loc theo `roles` (authz dua vao server 403).
2. [brute-force] Khoa dang nhap chi o client & reset khi restart — `login_screen.dart:27-28,171-178`.
3. [error-handling] `dart:developer log()` khong gate release — 8 file service (vd `vehicle_service.dart:143,198`, `image_service.dart:68,84,107`). Noi dung chi la thong bao loi, nen chuyen sang `logger`.

### LOW
- Upload S3 presigned (`image_service._uploadFileToS3`) khong pin (host khac portal) — van HTTPS.
- `options[i]!` trong cac man management la force-unwrap tren `enum.values` da guard -> an toan.
- File trung ten `storage_service.dart` (core vs vehicles) gay nham lan, nen doi ten.

## 4. Gaps / Critic

- Module/deep-dive docs (M1-M6, D1-D5) KHONG ton tai -> agent tu review goc, chua co review doc lap thu 2 cross-check.
- Khong chay duoc `flutter analyze`/build -> danh gia crash dua tren doc code tinh.
- Pinning chua co test xac thuc fingerprint that voi cert production; can verify `AMZ_CERT_SHA256` set dung khi build release.
- Chua ra soat CVE dependency trong `pubspec.yaml`.

---
*Generated by recon/completeness-critic review on release 3e6d332.*
